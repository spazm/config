#!/bin/sh
ctags -f tags \
    --recurse --totals               \
    --exclude=blib --exclude=.svn    \
    --exclude=.git --exclude='*~'    \
    --extra=q                        \
    --python-kinds=-i                \
    --languages=Perl,Python --langmap=Perl:+.t
